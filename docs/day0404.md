# Day 0404（2026-04-04）执行计划

## 已锁定决策

- 玩家档案独立页路径：`/profile`
- 首页入口文案：`玩家档案`
- 当日目标：4 项都做（含称号方案 B）
- 签到系统（周/月奖励）不纳入明日必做，转入 `docs` 总规划下一优先级

## 明日任务清单（按优先级）

### P0-1：玩家档案独立页落地（`/profile`）

- 内容迁移：昵称、头像（预设+上传裁剪）、每日任务、成长摘要、称号
- 首页改为摘要卡 + 按钮跳转（保留“继续日麻”快捷入口）
- 不引入后端，不改全局架构

验收：

- 首页可进入 `/profile`
- `/profile` 功能完整、数据与现有 localStorage 兼容
- 首页信息密度明显下降

### P0-2：称号系统 B（点数阈值解锁 + 手动切换）

- 新增称号定义与阈值（默认）：
  - 200：雀士
  - 600：雀杰
  - 1200：雀豪
- 达阈值自动解锁，`activeTitle` 支持手动切换并持久化

验收：

- 成长点变化后可正确解锁称号
- 当前称号切换后刷新页面不丢失

### P1-1：每日任务体验打磨

- 完成态反馈（轻提示）
- 领取后状态更清晰（不可重复领取）
- 展示“今日完成 x/3”

验收：

- 对局触发后任务进度可感知
- 奖励领取行为反馈清晰且不可重复领取

### P1-2：回归验证

- 专项链路：开局日麻 → 推进任务 → 领奖 → 成长点变化 → 称号变化
- 命令验证：
  - `pnpm run check`
  - `pnpm run test`

验收：

- 检查与测试全通过
- 关键手测链路可复现并稳定

## 时间预算（5h 内）

- `/profile` 页面迁移与首页瘦身：2h
- 称号系统 B：1.5h
- 任务体验细化：1h
- 回归与修正：0.5h

## 风险与处理

- 风险：页面迁移时状态耦合导致回归
- 处理：先复用现有模块（`playerProfile/dailyTasks/growth`），仅做页面重组，不重写业务核心

## 明日不做（已转总规划）

- 签到系统（每日签到、7 天奖励、30 天奖励、奖励背包扩展）