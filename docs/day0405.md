# Day 0405（2026-04-05）工作日志

## 今日已完成

### 1) 首页快捷入口体验完善

- 成就/签到/头像三入口弹层补齐
- 签到弹层显示当月日历（周标题 + 首周空位 + 今日高亮）
- 签到按钮支持短暂 `+10` 反馈
- 成就弹层增加“当前状态”进度条
- 头像弹层升级为玩家卡（称号徽章 + 最近游玩）
- 修复顶部层级遮挡问题（提升 header 层级）

### 2) 日麻页与首页入口统一

- 新增共享组件：`src/components/home/QuickAccessPanel.tsx`
- 首页改为复用共享组件，删除重复实现
- 日麻页 `GameHeader` 接入同一套三入口（桌面端显示），与首页行为一致

### 3) 校验结果

- 已执行：`pnpm run check`
- 结果：通过

### 4) P4 首批补齐（游戏感反馈）

- 新增全局“成就解锁”弹窗（与成长点数 Toast 并存，位置错开）
- 成就系统新增解锁事件派发（携带 `id / nameKey / rarity / points`）
- 日麻和了/流局结算弹窗新增“本局摘要”
  - 和了：赢家净变化 + 本局最大失分方
  - 流局：听牌家数/途中流局 + 本局最大净增方
- 成就弹窗监听机制收紧为单次注册，避免重复绑定

## 下一步建议（按优先级）

### P0：统一样式细节（低风险）

- 对齐首页与日麻页三入口按钮尺寸、间距、字体大小（尤其是 `compact` 模式）
- 对齐弹层圆角/阴影/宽度 token，避免“看起来像两套”

### P1：移动端补齐入口

- 目前日麻页快捷入口仅桌面端显示（`md:flex`）
- 可在移动端菜单中加入“成就/签到/头像”入口，保持功能可达

### P1：抽离文案常量

- `QuickAccessPanel` 里仍有少量中英硬编码文案（如最近游玩兜底文案）
- 收口到 i18n，避免后续重复维护
